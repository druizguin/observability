global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otel_biz_metrics
    honor_labels: true
    static_configs:
      - targets: ["otelcol:8889"]
    metric_relabel_configs:
      - target_label: metric_category
        replacement: business
